<h1>Book List</h1>
<ul>
  <% books.each do |book| %>
    <%= erb :book, locals:{book: book} %>
  <% end %>
</ul>
<h2>New Entry</h2>
<%= erb :new_book, locals: {users: users} %>

<h2>New User</h2>
<%= erb :new_user %>
